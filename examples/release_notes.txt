Version 0.0.3
- Initial Release with standard features to build C++/C projects
- Setup for multiple sources and project parts with own dependencies


Version 0.0.4
- add support for GIT repositories and cascade builds (when GIT repository has a .bodge file)
- add flag to just fetch and build dependencies from other repositories
- version number is now visible when use --version flag
- code improvements
